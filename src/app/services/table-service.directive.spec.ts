import { TableServiceDirective } from './table-service.directive';

describe('TableServiceDirective', () => {
  it('should create an instance', () => {
    const directive = new TableServiceDirective();
    expect(directive).toBeTruthy();
  });
});
